name: Upload Go test results

on: [push]

jobs:
  build:

    strategy:
      matrix:
        os: [ ubuntu-latest, windows-latest, macos-latest ]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21.x'  # Version Go unique
          
      - name: Install dependencies
        run: go get .
    
      - name: Test with Go
        run: go test -json > TestResults-${{ matrix.os }}.json
    
      - name: Upload Go test results
        uses: actions/upload-artifact@v4
        with:
          name: Go-results-${{ matrix.os }}
          path: TestResults-${{ matrix.os }}.json
    
      - name: Build
        run: go build -v ./...
    
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-artifact-${{ matrix.os }}
          path: go-sha256-${{ matrix.os }}
  
      - name: Create Release
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: $(date +'%Y%m%d')
          name: Release $(date +'%Y-%m-%d')
          files: |
            go-sha256-windows.exe
            go-sha256-linux
            go-sha256-macos
